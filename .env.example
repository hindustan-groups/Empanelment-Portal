# ─── FRONTEND (Vite) ─────────────────────────────────────────────────────────
# Copy this file to .env and fill in your real values.
# NEVER commit the actual .env file to git.

# URL of your deployed backend API server
VITE_API_BASE_URL=http://localhost:5000

# Admin login password (used on /internal-portal-login page)
VITE_ADMIN_PASSWORD=HindustanAdmin2026#

# Company GSTIN — shown on internal print documents only (admin panel)
VITE_COMPANY_GSTIN=08HYJPK8847M1ZC

# Company helpline phone — shown on internal documents
VITE_COMPANY_PHONE=+91-7597000601
