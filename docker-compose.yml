version: '3.8'

services:
  empanelment-portal:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hipro-empanelment-app
    restart: always
    ports:
      - "9000:9000"
    environment:
      - NODE_ENV=production
      - PORT=9000
      - ADMIN_PASSWORD=HindustanAdmin2026#
      - ADMIN_API_KEY=hipro_admin_vps_key_99201
      - EMAIL_USER=hindustanprojects0.2@gmail.com
      - EMAIL_APP_PASS=sbecchomfbrgkrwx
    volumes:
      - ./backend/empanelment.db:/app/backend/empanelment.db
      - ./backend/uploads:/app/backend/uploads
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:9000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
